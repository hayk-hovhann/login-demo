# dependencies
node_modules/

# build output
dist/
build/

# logs
*.log
npm-debug.log*

# env / secrets
.env
.env.*

# os / editor
.DS_Store
.vscode/
.idea/

# claude code context system (local only)
CLAUDE.md
.claude/
# curl cookie jars from manual auth testing — hold live session cookies.
# Glob both sides: the earlier `*jar.txt` missed suffixed names like
# awsjar-tls.txt, which is how a live connect.sid became untracked-but-visible.
*jar*.txt
# TLS material minted by infra/make-cert.sh — private keys, including the local
# root CA's. Regenerate with the script; never commit.
infra/certs/
